# Architecture Overview

この文書は、実装が進んだ後のコードベース構造を素早く把握するための補助文書です。
仕様や非目的は `docs/project-spec.md` を正本とし、この文書では現在の構成、責務分割、依存関係を扱います。

## 1. 位置づけ
- 対象: 実装済みまたは実装中のコードベース
- 目的: 人とAIエージェントが、構造と責務境界を短時間で把握できるようにする
- 更新タイミング: 構成、主要責務、依存関係、外部連携が変わったとき

## 2. プロジェクト構造
- 主要ディレクトリ:
  ```text
  <AIエージェントへ: ここに簡略化したディレクトリ構造を図示してください>
  ```
- 主要ファイル: `<主要ファイルの説明>`
- レイヤーやモジュールの分け方: `<レイヤーやモジュールの分け方の説明>`

## 3. 中心となる責務
- `<モジュール / コンポーネント名>`:
  - 役割: `<役割の説明>`
  - 主な依存先: `<主な依存先>`
  - 変更時の注意: `<変更時の注意事項>`

## 4. データや処理の流れ
- 主な入力: `<主な入力>`
- 中心ロジック: `<中心ロジック>`
- 外部出力: `<外部出力>`
- 境界がある箇所: `<境界がある箇所>`

## 5. 外部連携
- `<連携先>`:
  - 用途: `<用途>`
  - 接続方法: `<接続方法>`
  - 失敗時の扱い: `<失敗時の扱い>`

## 6. ビルド / テスト / デプロイ
- ビルド方法: `<ビルド方法>`
- テスト方法: `<テスト方法>`
- 配布またはデプロイ方法: `<配布またはデプロイ方法>`
- CI で担保していること: `<CI で担保していること>`

## 7. 制約と今後の見直し候補
- 現在の制約: `<現在の制約>`
- 技術的負債: `<技術的負債>`
- 今後見直す可能性が高い箇所: `<今後見直す可能性が高い箇所>`
